version: "3.8"

networks:
  dev:
    name: dev

services:

  apache:
    image: httpd:2.4.51-alpine3.14
    working_dir: /app
    networks:
      - dev
    ports:
      - 80:80
    volumes:
      - ./configs/apache:/usr/local/apache2/conf
      - ./docs:/app
